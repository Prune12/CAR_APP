package com.backend.car_app.Service;

/*import com.backend.car_app.Service.implementation.adapter.PDFDocumentAdapter;
import com.backend.car_app.models.PDFDocument;
import org.springframework.beans.factory.annotation.Autowired;

public class DocumentService {
    private PDFDocumentAdapter adapter;

    @Autowired
    public DocumentService(PDFDocumentAdapter adapter) {
        this.adapter = adapter;
    }

    public void createAndPrintDocument(String filePath) {
        PDFDocument pdfDocument = new PDFDocument(filePath);
        adapter = new PDFDocumentAdapter(pdfDocument);
        adapter.print();
    }
}*/
