package com.backend.car_app.controller;

/*import com.backend.car_app.Service.DocumentService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class DocumentController {

    private DocumentService documentService;


    @PostMapping("/document")
    public String createAndPrintDocument(@RequestParam String filePath) {
        documentService.createAndPrintDocument(filePath);
        return "Document créé et imprimé";
    }
}*/
